package ca.mcmaster.se2aa4.island.team012.DroneComponents;

/**
 * This enum represents the different commands that the drone can execute.
 */
public enum Command{
    MOVE, SCAN, TURN_AROUND, ECHO, TURN, ECHO_AROUND, NEUTRAL 
}